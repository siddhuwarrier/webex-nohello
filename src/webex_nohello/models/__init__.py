"""Models, one class per module, grouped by what they are for.

`auth` holds the credential and sign-in types, `webex` the shapes Webex returns, and
`errors` the exception hierarchy. Import the class directly from its module rather than
from here, so a reader can see at the import line exactly where a type is defined.
"""
